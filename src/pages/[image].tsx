import type { NextPage } from 'next';

type ImageProps = {
    
};

const Image: NextPage<ImageProps> = ({}) => {
    return (
        <div>
            
        </div>
    );
}

export default Image;